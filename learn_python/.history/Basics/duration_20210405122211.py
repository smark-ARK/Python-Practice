dStr = input(
    'Enter the number of days,hours,minutes,seconds by seperating with spaces: ')
dspl = dStr.split()
print(dspl[0])
days = dspl[0]
hours = dspl[1]
mins = dspl[2]
secs = dspl[3]
